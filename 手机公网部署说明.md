# 手机随时访问

项目已适配 Streamlit Community Cloud。部署成功后会得到固定的 HTTPS 地址，
手机不必与电脑连接同一 Wi-Fi，电脑关机后也能使用。

## 发布步骤

1. 在 GitHub 新建一个私有仓库，把本项目代码上传；不要上传 `.streamlit/secrets.toml`。
2. 打开 `https://share.streamlit.io/`，使用 GitHub 登录。
3. 点击 **Create app**，选择刚才的仓库。
4. Main file path 填写 `streamlit_app.py`。
5. 在 **Advanced settings → Secrets** 中填写：

   ```toml
   OPENAI_API_KEY = "你的 OpenAI API Key"
   ```

6. 点击 Deploy。完成后将生成的 HTTPS 地址在手机浏览器打开。
7. iPhone 使用 Safari 的“分享 → 添加到主屏幕”；安卓使用 Chrome 的
   “菜单 → 添加到主屏幕”。之后即可像普通 App 一样点击图标进入。

## 数据说明

本地版本继续使用电脑上的 SQLite 数据库。Community Cloud 的本地磁盘可能在
应用重启时重置，因此正式长期使用前，素材数据建议再接入持久化云数据库与对象存储。
